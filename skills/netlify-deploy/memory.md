# Memoria Persistente de netlify-deploy

Este archivo mantiene el estado persistente, aprendizajes, y decisiones clave (Graph State) para la skill `netlify-deploy`. La skill debe leerlo y actualizarlo constantemente.

## Contexto Actual
- Recién inicializado.
